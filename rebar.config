%% vim: set filetype=erlang:

{require_otp_vsn, "R15B01"}.

{port_env, [
              {"CFLAGS", "$CFLAGS -DGEOIPDATADIR=\"\\\"\\\"\""},
              {"(linux|solaris|freebsd|netbsd|openbsd|dragonfly|darwin)", "LDFLAGS", "$LDFLAGS -lstdc++"}
            ]}.

{port_specs, [
                {"priv/geoip.so", ["c_src/*.c", "c_src/*.cpp"]}
            ]}.
